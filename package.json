{
  "name": "calculatorui",
  "version": "1.0.0",
  "description": "My calc now have a interface",
  "main": "index.js",
  "scripts": {
    "test": "echo \"test succeed\"",
    "start": "run-p serve watch:css",
    "scss": "node-sass --output-style compressed -o ./ src/",
    "serve": "browser-sync start --server --files './*.css, **/*.html'",
    "watch:css": "onchange 'src/scss' -- npm run scss"
  },
  "author": "Pail4",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.7",
    "node-sass": "^6.0.1",
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0"
  }
}
